#include "main.h"
/**
 * _abs - Computes the absolute value of an integer
 * @n: The integer that its abs to be computed
 *Return: Alway 0.
 */
int _abs(int)
{
int n;
_abs(n);
_putchar("%d\n", n);
return (0);
}
